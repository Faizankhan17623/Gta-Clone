const COSTS=[500,900,1400,2000,2800];
export function createSkills({hud,game,shooting,player,controls,storage=localStorage}){let levels;try{levels=JSON.parse(storage.getItem('arenaSkills'));}catch{}levels??={damage:0,vitality:0,mobility:0,reload:0};function buy(id){const level=levels[id]??0;if(level>=5)return hud.toast('Skill is maxed');const cost=COSTS[level];if(!game.spendScore(cost))return hud.toast('Not enough score','danger');levels[id]++;storage.setItem('arenaSkills',JSON.stringify(levels));if(id==='damage')shooting.upgrade('damage');if(id==='reload')shooting.upgrade('reload');if(id==='vitality')game.setMaxHealth(game.state.maxHealth+15);if(id==='mobility')player.setSpeedMultiplier(1+levels[id]*.05);hud.toast(`${id.toUpperCase()} LEVEL ${levels[id]}`,'good');open();}function open(){if(document.pointerLockElement)document.exitPointerLock();hud.showSkillTree(levels,COSTS,buy,close);}function close(){hud.hideModal();controls.lock();}document.addEventListener('keydown',e=>{if(e.code==='KeyK'&&game.state.alive)open();});return{levels,buy,open};}
