export function createMinimap({player,enemies}){const canvas=document.createElement('canvas');canvas.id='minimap';canvas.width=180;canvas.height=180;document.body.appendChild(canvas);const c=canvas.getContext('2d'),scale=3.25,center=90;function dot(x,z,color,r=3){c.fillStyle=color;c.beginPath();c.arc(center+x*scale,center+z*scale,r,0,Math.PI*2);c.fill();}function update(){c.clearRect(0,0,180,180);c.fillStyle='rgba(3,12,20,.78)';c.fillRect(0,0,180,180);c.strokeStyle='#456070';c.strokeRect(center-25*scale,center-25*scale,50*scale,50*scale);for(const e of enemies.list)dot(e.position.x,e.position.z,e.userData.enemy.type==='boss'?'#d875ff':'#ff5964',e.userData.enemy.type==='boss'?6:3);const p=player.object.position;dot(p.x,p.z,'#55e6ff',4);const yaw=player.object.rotation.y;c.strokeStyle='#55e6ff';c.beginPath();c.moveTo(center+p.x*scale,center+p.z*scale);c.lineTo(center+(p.x+Math.sin(yaw)*3)*scale,center+(p.z+Math.cos(yaw)*3)*scale);c.stroke();}return{update};}
