import*as THREE from'three';
export function createEnvironment(scene){let time=.25,weather='clear',weatherTimer=45,flash=0;const sun=scene.getObjectByName('sun'),ambient=scene.getObjectByName('ambient');const count=450,pos=new Float32Array(count*3);for(let i=0;i<count;i++){pos[i*3]=(Math.random()-.5)*55;pos[i*3+1]=Math.random()*25;pos[i*3+2]=(Math.random()-.5)*55;}const geo=new THREE.BufferGeometry();geo.setAttribute('position',new THREE.BufferAttribute(pos,3));const rain=new THREE.Points(geo,new THREE.PointsMaterial({color:0xaedcff,size:.08,transparent:true,opacity:.65}));rain.visible=false;scene.add(rain);function setWeather(v){weather=v;rain.visible=v==='rain';weatherTimer=45;}function update(dt){time=(time+dt/180)%1;const daylight=Math.max(.08,Math.sin(time*Math.PI));scene.background.setHSL(.56,.45,.12+daylight*.55);scene.fog.color.copy(scene.background);if(sun){sun.intensity=daylight*1.35;sun.position.x=Math.cos(time*Math.PI*2)*35;sun.position.y=8+daylight*38;}if(ambient)ambient.intensity=.18+daylight*.38;weatherTimer-=dt;if(weatherTimer<=0)setWeather(weather==='clear'?'rain':'clear');if(weather==='rain'){const a=geo.attributes.position.array;for(let i=1;i<a.length;i+=3){a[i]-=dt*20;if(a[i]<0)a[i]=25;}geo.attributes.position.needsUpdate=true;if(Math.random()<dt*.045)flash=.12;}if(flash>0){flash-=dt;if(ambient)ambient.intensity=3;} }return{update,setWeather,get weather(){return weather;}};}
